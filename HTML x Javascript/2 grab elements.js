//Grab elements by ID

const title = document.getElementById("title"); //-> Grab the element that have id="title"

console.dir(title); //-> Show the title element in the console

title.innerText = "Got you!"; // -> Change the text of the title element

console.log(title.id);
console.log(title.className);